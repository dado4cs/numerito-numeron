# semana1

OBEJTIVO: consumir y crear rest apis

API: interfaz           

Rest api: tipo de api (se usa cuando cumple los principios Rest

Principios

- Cliente-servidor
- Stateless
    - contexto para poder entendernos, cada request e maneja independientemente
    - ejm: metodos de git
- Uniform Interface
- Code On demand
- Cache
    - donde se almacena
    - cliente → browser → server
    - cliente→ (hace request) Server (busca en la base de datos
    - la memoria cache se encuentra en el browser, y se almacena cuando se hacen consultas frecuentes

URI 

identificador unico de recursos que se encuentra antes del URL

ejm: isbn, mail, https

URL

indica la ubicació de cierto recurso y método de acceso 

uri : URL y URN

PATH:

url base

protocolo→ subdomini→ dominio→ ruta→ parametro → etiqueta (a veces no está)

Metodos HTTP RESTful APIs

- GET: retrieve data
- PUT: submit new data
- POST : update data
- DELETE: remove data
- patch: actualizacion parcial

**endpoint**: ubi digital donde una interfaz recibe solicitudes de api para recursos en su server. para que sea endpoint necesitamos al metodo 

método: te dice como estás interactuando.

Idempotencia : multiples solicitudes del mismo metodo mismo efecto que una sola solicitud 

web scraping: no se comunica directamente con el server

api reduce tiempo de respuesta 

#### codigo

200-205: todo correcto 
#### SOAP APIs

- Usa XML para intercambiar info
- Rigido pero bueno para operaciones complejas

Las APIs más modernas usan REST porque es simple, flexible y trabaja con web tech.

## Request

todo request incluye:

1. un endpoint (la url donde mandas la request)
2. un método http ( que accion tomaras :get,post,etc)
3. opcional: parametro , cuerpo, dependiendo de la API

#### ¿Qué esta incluido en  un api response?

Un status code, response body y headers

- cada post debe tener un body
- api key:
- la autorizacion va en el header

```python
def prueba_funct(page, pagesize, role=”student”): 
```
